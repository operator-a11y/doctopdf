# Google OAuth secrets — never commit these
client_secret.json
token.json
# Per-account tokens + the accounts index (multi-account support)
tokens/
accounts.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# py2app build output
build/
dist/

# macOS
.DS_Store

# RAG vector store — a local artifact, not the (human-readable) git audit trail
.vectorstore/
doctopdf-meta.json
