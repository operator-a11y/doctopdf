# Native macOS menu bar via PyObjC (AppKit). rumps was dropped: it relies on
# APIs deprecated since macOS 10.10 and its menu does not render on macOS 26's
# NSSceneStatusItem.
pyobjc-framework-Cocoa>=10.0
google-api-python-client>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth>=2.0.0
# Web-page monitoring (fetch + extract/denoise + JS render).
requests>=2.0.0
trafilatura>=1.6.0
beautifulsoup4>=4.11.0
lxml>=4.9.0
playwright>=1.40.0          # JS-rendered pages; also run: playwright install chromium
